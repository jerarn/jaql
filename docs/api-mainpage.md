# JAQL API Reference

API documentation for public headers under `include/jaql/`.

Narrative documentation (architecture, build system, contribution guide) lives in the
repository `docs/` tree and the root README.
